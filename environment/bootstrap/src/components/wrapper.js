import React from 'react'
// import PageAssets from './containers/page-assets.js'

// Use this to wrap your route components with other components that is common
// to all pages.
const Wrapper = (props) => (
    <div>
        {props.children}
    </div>
)

export default Wrapper
